{
  "name": "@glass/viewer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/hub-client.js",
  "types": "./dist/hub-client.d.ts",
  "exports": {
    "./hub-client": {
      "types": "./dist/hub-client.d.ts",
      "default": "./dist/hub-client.js"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build:lib": "tsc -p tsconfig.lib.json",
    "build": "tsc -p tsconfig.lib.json && vite build"
  },
  "dependencies": {
    "@glass/protocol": "workspace:*",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-updater": "^2",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
