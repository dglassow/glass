{
  "name": "glass",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "node tests/m1-acceptance.mjs && node tests/m2-acceptance.mjs && node tests/m3-viewer.mjs && node tests/p2m1-auth.mjs",
    "clean": "tsc -b --clean && rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.12.0"
}
