node_modules/
dist/
dist-web/
*.tsbuildinfo

# Tauri (desktop shell) build artifacts
packages/desktop/src-tauri/target/
packages/desktop/src-tauri/gen/

# Terraform — state and account-specific backend/vars never go in the public repo
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/backend.hcl
infra/**/*.tfvars
infra/**/*.auto.tfvars

# Secrets and instance config never belong in this repo.
# All of it lives in the backup bundle instead.
.env
.env.*
*.pem
*.key
*.p12
*.mobileprovision
glass.config.json
/config/local/

.DS_Store
*.log
logs/
