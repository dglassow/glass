{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window: core defaults plus the Glass native bridge commands (proxied browser launch, app version).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-launch-proxied-browser",
    "allow-app-version"
  ]
}
